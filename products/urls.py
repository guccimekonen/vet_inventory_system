from django.urls import path
from . import views  # make sure you have views.py in products

app_name = "products"

urlpatterns = [
    # Example placeholder
    # path('', views.product_list, name='list'),
]
